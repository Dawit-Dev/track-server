import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const signinScreen = () => {
  return (
    <View>
      <Text style={{ fontSize: 45 }}>signin Screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({})

export default signinScreen
